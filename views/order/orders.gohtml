{{define "content"}}
<div class="orders-container">
        <h1>{{.Title}}</h1>

    {{if .Orders}}
    <table class="orders-table">
        <thead>
            <tr>
                <th>Order ID</th>
                <th>User</th>
                <th>Date</th>
                <th>Total Price</th>
                <th>Status</th>
                <th>Details</th>
            </tr>
        </thead>
        <tbody>
            {{range .Orders}}
            <tr>
                <td>{{.ID}}</td>
                <td>{{if .UserID.Valid}}{{.UserID.Int64}}{{else}}Guest{{end}}</td>
                <td>{{.Date}}</td>
                <td>${{printf "%.2f" .TotalPrice}}</td>
                <td>{{.Status}}</td>
                                <td>
                                    {{if .UserID.Valid}}
                                        {{if eq $.Title "All Orders"}}
                                            <a href="/order/{{.ID}}?all=1">View Details</a>
                                        {{else if eq $.Title "My Orders"}}
                                            <a href="/order/{{.ID}}?my=1">View Details</a>
                                        {{else}}
                                            <a href="/order/{{.ID}}">View Details</a>
                                        {{end}}
                                    {{else}}
                                        <span title="Guest order - no details">N/A</span>
                                    {{end}}
                                </td>
            </tr>
            {{end}}
        </tbody>
    </table>
    {{else}}
    <p>You have no orders yet.</p>
    {{end}}
</div>
{{end}}